['ant', 'bat', 'caterpillar'].count do |str|
  str.length < 4
end

# Count evaluated the blocks return value as a boolean and counts the number of elements
# which return a true value. In this case the count method returns 2.